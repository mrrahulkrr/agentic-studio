venv/
.env
# Credential JSON (GCP service-account keys download under their project id).
# No tracked .json lives at the repo root — all of them are under frontend/.
/*.json
gcp-credentials.json
service-account-credentials.json
__pycache__/
*.pyc
.claude/
openspec/
.idea/
node_modules/
.next/
*.tsbuildinfo